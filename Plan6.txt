I have created the following plan after thorough exploration and analysis of the codebase. Follow the below plan verbatim. Trust the files and references. Do not re-verify what's written in the plan. Explore only when absolutely necessary. First implement all the proposed file changes and then I'll review all the changes together at the end.

### Observations

The codebase has a well-structured MERN stack application with MongoDB/Mongoose. I've analyzed the existing patterns for models (User, Follow, Snippet, SnippetComment), controllers with consistent error handling and authentication, routes with CSRF protection and validation, and comprehensive middleware for rate limiting, authentication, and validation. The Follow system already exists and provides a good foundation for notification triggers. The pagination utilities and authentication middleware are robust and ready to be reused. The constants file can be extended for notification-related configurations.

### Approach

I'll implement a comprehensive notification system by creating a Notification model with proper indexing for performance, adding notification creation logic to existing controllers (follow, snippet, comment), creating dedicated notification endpoints with proper authentication and rate limiting, and providing real-time unread count functionality. The implementation will follow existing patterns for consistency, use efficient bulk operations for fan-out scenarios, and include proper validation and error handling. I'll leverage the existing Follow model for efficient follower queries and maintain backward compatibility.

### Reasoning

I explored the repository structure to understand the codebase organization. I examined existing models to see patterns for TypeScript interfaces, static methods, validation, and indexing. I reviewed controllers to understand error handling, authentication, and response patterns. I analyzed routes to see middleware usage and mounting patterns. I checked validation middleware to understand express-validator patterns and sanitization. I reviewed rate limiting, authentication, and pagination utilities to understand the exact patterns to follow. This gave me a comprehensive understanding of how to implement the notification system following all existing conventions.

## Mermaid Diagram

sequenceDiagram
    participant User as User A
    participant FollowController
    participant NotificationHelper
    participant NotificationModel
    participant Database
    participant UserB as User B
    participant NotificationController

    Note over User,NotificationController: Follow Notification Flow
    User->>FollowController: POST /follows/users/:id/follows
    FollowController->>Database: Create follow relationship
    Database-->>FollowController: Follow created
    FollowController->>NotificationHelper: notifyOnFollow(userA.id, userB.id)
    NotificationHelper->>NotificationModel: createNotification(userB.id, 'FOLLOW', {followerId: userA.id})
    NotificationModel->>Database: Insert notification
    Database-->>NotificationModel: Notification created
    FollowController-->>User: Follow success response

    Note over User,NotificationController: Snippet Creation Fan-out
    User->>SnippetController: POST /snippets
    SnippetController->>Database: Create snippet
    Database-->>SnippetController: Snippet created
    SnippetController->>Database: Query followers of author
    Database-->>SnippetController: Follower IDs array
    SnippetController->>NotificationHelper: notifyOnSnippetCreation(snippet.id, author.id, followerIds)
    NotificationHelper->>NotificationModel: createBulkNotifications(followerIds, 'NEW_SNIPPET', {snippetId, authorId})
    NotificationModel->>Database: Bulk insert notifications
    Database-->>NotificationModel: Notifications created
    SnippetController-->>User: Snippet creation success

    Note over User,NotificationController: Real-time Unread Count
    UserB->>NotificationController: GET /notifications/unread-count
    NotificationController->>NotificationModel: getUnreadCount(userB.id)
    NotificationModel->>Database: COUNT({userId: userB.id, isRead: false})
    Database-->>NotificationModel: Count result
    NotificationModel-->>NotificationController: Unread count
    NotificationController-->>UserB: {count: 3}

    Note over User,NotificationController: Mark Notifications as Read
    UserB->>NotificationController: PATCH /notifications/:id/read
    NotificationController->>NotificationModel: markAsRead(notificationId, userB.id)
    NotificationModel->>Database: Update notification isRead = true
    Database-->>NotificationModel: Notification updated
    NotificationController-->>UserB: Mark as read success

## Proposed File Changes

### code-craft-backend\src\utils\constants.ts(MODIFY)

Add notification-related constants to the API_CONSTANTS object. Include `NOTIFICATION_TYPES` enum with values: 'FOLLOW', 'COMMENT', 'REPLY', 'NEW_SNIPPET'. Add `MAX_NOTIFICATIONS_PER_PAGE: 50` for pagination limits, `NOTIFICATION_DATA_MAX_SIZE: 1000` to limit the data field size, and `MAX_NOTIFICATION_AGE_DAYS: 90` for future cleanup functionality. Add rate limiting configuration for notifications in RATE_LIMITS object: `NOTIFICATION_FETCH: { windowMs: 60 * 1000, max: 60 }` for fetching notifications.

### code-craft-backend\src\models\Notification.ts(NEW)

References: 

- code-craft-backend\src\models\Follow.ts
- code-craft-backend\src\utils\constants.ts(MODIFY)

Create a new Notification model following the same patterns as existing models like `c:/Users/dhruv/Documents/VS-CODE/CODE-EDITOR/code-craft-backend/src/models/Follow.ts`. Define INotification interface with `userId` (ObjectId reference to User - the recipient), `type` (enum: FOLLOW, COMMENT, REPLY, NEW_SNIPPET), `data` (mixed object containing relevant IDs like snippetId, commentId, followerId), `isRead` (boolean, default false), and timestamps. Include INotificationStatics interface with static methods: `createNotification()` for single notification creation, `createBulkNotifications()` for fan-out scenarios, `markAsRead()`, `markAllAsRead()`, `getUnreadCount()`, and `getUserNotifications()` with pagination. Create schema with proper validation, indexes on `{ userId: 1, isRead: 1, createdAt: -1 }` for efficient unread queries, and `{ createdAt: -1 }` for cleanup. Add data field size validation and sanitization to prevent XSS.

### code-craft-backend\src\models\index.ts(MODIFY)

References: 

- code-craft-backend\src\models\Notification.ts(NEW)

Add the new Notification model export to the models index file. Include the export statement for Notification, INotification, and NotificationModel following the same pattern as existing model exports like Follow and User.

### code-craft-backend\src\utils\notificationHelper.ts(NEW)

References: 

- code-craft-backend\src\models\Notification.ts(NEW)
- code-craft-backend\src\utils\logger.ts

Create a notification helper utility with functions for creating notifications. Include `createNotification(userId, type, data)` for single notifications and `createBulkNotifications(userIds, type, data)` for fan-out scenarios. Add `notifyOnFollow(followerId, followingId)` to create follow notifications, `notifyOnComment(snippetId, commentId, snippetOwnerId, commenterId)` for comment notifications, `notifyOnReply(parentCommentId, replyId, parentCommentOwnerId, replierId)` for reply notifications, and `notifyOnSnippetCreation(snippetId, authorId, followerIds)` for new snippet notifications. Include proper error handling, logging, and validation to prevent self-notifications (user doesn't get notified of their own actions). Use the Notification model's static methods for database operations.

### code-craft-backend\src\controllers\followController.ts(MODIFY)

References: 

- code-craft-backend\src\utils\notificationHelper.ts(NEW)
- code-craft-backend\src\models\Follow.ts

Update the `toggleFollow` function to create notifications when a new follow relationship is established. Import the notification helper and call `notifyOnFollow(followerId, followingId)` after a successful follow action (when isFollowing becomes true). Ensure the notification is only created for new follows, not unfollows. Add proper error handling so that notification creation failures don't break the follow functionality - log errors but continue with the follow response. Follow the same patterns as existing controller functions for error handling and logging.

### code-craft-backend\src\controllers\snippetController.ts(MODIFY)

References: 

- code-craft-backend\src\utils\notificationHelper.ts(NEW)
- code-craft-backend\src\models\Follow.ts
- code-craft-backend\src\models\Snippet.ts

Update the `createSnippet` function to notify followers when a new snippet is created. After successful snippet creation, query the Follow model to get all follower IDs using `Follow.find({followingId: req.user.id}).select('followerId').lean()`. Then call the notification helper's `notifyOnSnippetCreation(snippet._id, req.user.id, followerIds)` to create bulk notifications. Handle the case where the user has no followers gracefully. Add proper error handling so notification creation failures don't break snippet creation - log errors but continue with the snippet creation response. Use the existing patterns for database queries and error handling.

### code-craft-backend\src\controllers\commentController.ts(MODIFY)

References: 

- code-craft-backend\src\utils\notificationHelper.ts(NEW)
- code-craft-backend\src\models\Snippet.ts
- code-craft-backend\src\models\SnippetComment.ts

Update the `addComment` and `addReply` functions to create notifications. In `addComment`, after successful comment creation, check if it's a top-level comment and notify the snippet owner using `notifyOnComment(snippetId, comment._id, snippet.userId, req.user.id)`. Ensure the commenter doesn't get notified of their own comment. In `addReply`, notify the parent comment owner using `notifyOnReply(parentCommentId, reply._id, parentComment.userId, req.user.id)`. Also notify the snippet owner if they're different from the parent comment owner. Add proper error handling so notification failures don't break comment creation. Follow existing patterns for database queries and error handling.

### code-craft-backend\src\controllers\notificationController.ts(NEW)

References: 

- code-craft-backend\src\controllers\userController.ts
- code-craft-backend\src\models\Notification.ts(NEW)
- code-craft-backend\src\utils\pagination.ts

Create a notification controller following the same patterns as `c:/Users/dhruv/Documents/VS-CODE/CODE-EDITOR/code-craft-backend/src/controllers/userController.ts`. Implement `getNotifications` function with pagination support, optional `unreadOnly` query parameter, and proper authentication checks. Add `markAsRead` function to mark a specific notification as read with ownership validation. Include `markAllAsRead` function to mark all user notifications as read. Add `getUnreadCount` function for real-time badge updates that returns a simple count. Use the Notification model's static methods for database operations. Include proper error handling with catchAsync, HTTP_STATUS constants, and ERROR_CODES. Add comprehensive logging for all operations. Ensure all functions require authentication and validate user ownership of notifications.

### code-craft-backend\src\middleware\validation.ts(MODIFY)

References: 

- code-craft-backend\src\utils\constants.ts(MODIFY)

Add notification-specific validation middleware. Create `validateNotificationFetch` for the notifications list endpoint with optional `unreadOnly` boolean query parameter and standard pagination validation. Add `validateMarkAsRead` for the mark-as-read endpoint with ObjectId validation for the notification ID parameter. Include `validateNotificationData` helper for validating notification data field size and content to prevent XSS. Follow the same patterns as existing validation middleware using express-validator, with proper error messages and sanitization. Use the existing `handleValidationErrors` middleware and follow the same structure as other validation functions.

### code-craft-backend\src\routes\notificationRoutes.ts(NEW)

References: 

- code-craft-backend\src\routes\userRoutes.ts
- code-craft-backend\src\controllers\notificationController.ts(NEW)
- code-craft-backend\src\middleware\validation.ts(MODIFY)

Create notification routes following the same patterns as `c:/Users/dhruv/Documents/VS-CODE/CODE-EDITOR/code-craft-backend/src/routes/userRoutes.ts`. Set up routes: `GET /` for getting notifications with `requireAuth`, `validateNotificationFetch`, `validatePagination`, and `generalLimiter` middleware. Add `PATCH /:id/read` for marking single notification as read with `requireAuth`, `validateObjectId('id')`, and `generalLimiter`. Include `PATCH /read-all` for marking all as read with `requireAuth` and `generalLimiter`. Add `GET /unread-count` for getting unread count with `requireAuth` and `generalLimiter`. Apply proper middleware chains following existing patterns with authentication, validation, and rate limiting. Import all necessary controller functions and middleware.

### code-craft-backend\src\routes\index.ts(MODIFY)

References: 

- code-craft-backend\src\routes\notificationRoutes.ts(NEW)

Import the new notificationRoutes and mount it in the main router. Add the import statement for notificationRoutes at the top with other route imports. Mount the notification routes using `router.use('/notifications', verifyCsrfToken, notificationRoutes);` following the same pattern as other protected routes. Update the API documentation in the `/docs` endpoint to include the new notification endpoints in the endpoints object, describing: list notifications, mark as read, mark all as read, and unread count endpoints with their authentication requirements and response formats.