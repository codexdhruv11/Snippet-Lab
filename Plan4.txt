I have created the following plan after thorough exploration and analysis of the codebase. Follow the below plan verbatim. Trust the files and references. Do not re-verify what's written in the plan. Explore only when absolutely necessary. First implement all the proposed file changes and then I'll review all the changes together at the end.

### Observations

The backend has a complete comment threading system with parentCommentId support, depth limiting, and comprehensive endpoints for threaded comments, replies, and nested structures. The notification system is fully implemented with CRUD operations, unread counts, and proper pagination. The frontend currently has flat comment components that need threading support, a header ready for notification integration, and solid foundations with React Query, Zustand stores, and Shadcn/ui components. The existing patterns for API integration, component structure, and state management provide clear guidance for implementation. All necessary UI components are available, and the authentication system is robust.

### Approach

I'll implement a comprehensive comment threading and notification system by extending the existing frontend components to support nested replies, adding a real-time notification dropdown with unread badges, and integrating with the robust backend APIs already in place. The implementation will follow existing patterns for API integration, component structure, and state management while providing an intuitive GitHub-style threading UI and a polished notification experience. I'll prioritize performance through lazy loading of replies, efficient React Query caching, and optimized re-renders.

### Reasoning

I explored the repository structure to understand the codebase organization. I examined the current comment components (CommentList, CommentItem) to see the flat structure and existing patterns, reviewed the backend comment and notification controllers to understand the complete threading and notification APIs available, analyzed the Header component to see how to integrate notification functionality, checked the API client and constants to understand data fetching patterns, reviewed the auth and UI stores to understand state management, and examined available Shadcn/ui components to confirm all necessary primitives are available. This gave me a comprehensive understanding of both the robust backend infrastructure and the frontend patterns to follow.

## Mermaid Diagram

sequenceDiagram
    participant User
    participant Header
    participant NotificationDropdown
    participant NotificationAPI
    participant CommentList
    participant CommentItem
    participant ReplyBox
    participant CommentAPI
    participant Database

    Note over User,Database: Notification System Flow
    User->>Header: Page load
    Header->>NotificationAPI: useUnreadCount() hook
    NotificationAPI->>Database: GET /notifications/unread-count
    Database-->>NotificationAPI: {count: 3}
    NotificationAPI-->>Header: Display badge with count
    User->>Header: Click notification bell
    Header->>NotificationDropdown: Open dropdown
    NotificationDropdown->>NotificationAPI: getNotifications(page=1)
    NotificationAPI->>Database: GET /notifications?page=1&limit=20
    Database-->>NotificationAPI: Paginated notifications
    NotificationAPI-->>NotificationDropdown: Display notification list
    User->>NotificationDropdown: Click notification item
    NotificationDropdown->>NotificationAPI: markAsRead(notificationId)
    NotificationAPI->>Database: PATCH /notifications/:id/read
    Database-->>NotificationAPI: Notification marked as read
    NotificationAPI-->>Header: Update unread count (optimistic)

    Note over User,Database: Comment Threading Flow
    User->>CommentList: View snippet comments
    CommentList->>CommentAPI: getThreadedComments(snippetId)
    CommentAPI->>Database: GET /snippets/:id/comments/threaded
    Database-->>CommentAPI: Nested comment tree
    CommentAPI-->>CommentList: Threaded comments data
    CommentList->>CommentItem: Render with replies array
    CommentItem->>CommentItem: Recursively render nested replies
    User->>CommentItem: Click "Reply" button
    CommentItem->>ReplyBox: Show reply form
    User->>ReplyBox: Type reply and submit
    ReplyBox->>CommentAPI: createReply(snippetId, parentCommentId, content)
    CommentAPI->>Database: POST /snippets/:id/comments/:commentId/replies
    Database-->>CommentAPI: Reply created + notification triggered
    CommentAPI-->>ReplyBox: Reply success
    ReplyBox->>CommentList: onReplySuccess callback
    CommentList->>CommentList: Update comment tree (optimistic)
    CommentList->>CommentAPI: Invalidate threaded comments query
    CommentAPI-->>CommentList: Refresh comment tree

## Proposed File Changes

### code-craft-frontend\src\lib\constants.ts(MODIFY)

Add notification-related endpoints to the API_ENDPOINTS object. Include NOTIFICATIONS section with endpoints for list notifications (`/notifications`), mark as read (`/notifications/:id/read`), mark all as read (`/notifications/read-all`), and unread count (`/notifications/unread-count`). Update COMMENTS section to include threaded endpoints: threaded comments (`/snippets/:id/comments/threaded`), add reply (`/snippets/:id/comments/:commentId/replies`), get replies (`/comments/:id/replies`), and comment thread (`/comments/:id/thread`). Add notification-related constants to API_LIMITS: `MAX_COMMENT_DEPTH: 5`, `NOTIFICATION_POLL_INTERVAL: 30000` (30 seconds), and `MAX_NOTIFICATIONS_PER_PAGE: 20`.

### code-craft-frontend\src\types\api.ts(MODIFY)

Add comprehensive TypeScript interfaces for the threading and notification systems. Create `ThreadedComment` interface extending the existing Comment with `parentCommentId?: string`, `replies?: ThreadedComment[]`, `replyCount: number`, and `depth: number`. Add `Notification` interface with `_id: string`, `type: 'FOLLOW' | 'COMMENT' | 'REPLY' | 'NEW_SNIPPET'`, `data: Record<string, any>`, `isRead: boolean`, `createdAt: string`, and `updatedAt: string`. Include `NotificationResponse` interface for paginated notifications and `UnreadCountResponse` interface with `count: number`. Add `CreateReplyRequest` interface with `content: string` and `parentCommentId: string`. These interfaces ensure type safety across all new components and API calls.

### code-craft-frontend\src\lib\api.ts(MODIFY)

References: 

- code-craft-frontend\src\lib\constants.ts(MODIFY)
- code-craft-frontend\src\types\api.ts(MODIFY)

Extend the commentApi object with threading support functions. Add `getThreadedComments(snippetId, params)` for fetching nested comment structures, `createReply(snippetId, parentCommentId, content)` for adding replies to comments, `getReplies(commentId, page, limit)` for paginated replies, and `getCommentThread(commentId)` for full thread with nested replies. Create a new `notificationApi` object with `getNotifications(page, limit, unreadOnly)` for fetching user notifications, `markAsRead(notificationId)` for marking single notifications as read, `markAllAsRead()` for bulk marking, and `getUnreadCount()` for real-time badge updates. All functions should follow existing error handling patterns and include proper TypeScript typing.

### code-craft-frontend\src\hooks\useNotifications.ts(NEW)

References: 

- code-craft-frontend\src\lib\api.ts(MODIFY)
- code-craft-frontend\src\lib\constants.ts(MODIFY)

Create a custom hook for notification management that provides real-time updates and efficient caching. The hook should use React Query to fetch notifications with `refetchInterval` for polling, manage unread count with automatic updates, provide mutations for marking notifications as read with optimistic updates, handle error states and retry logic, and expose loading states for UI feedback. Include functions like `useNotifications(unreadOnly)` for paginated lists, `useUnreadCount()` for header badge, `useMarkAsRead()` and `useMarkAllAsRead()` mutations. The hook should invalidate relevant queries after mutations and provide a clean API for components to consume notification data efficiently.

### code-craft-frontend\src\components\ui\separator.tsx(NEW)

Create a Shadcn/ui Separator component using `npx shadcn-ui@latest add separator`. This component will be used in the notification dropdown to separate different notification types and provide visual hierarchy. The separator should support horizontal and vertical orientations and follow standard Shadcn/ui patterns for styling and accessibility.

### code-craft-frontend\src\components\notification\NotificationItem.tsx(NEW)

References: 

- code-craft-frontend\src\components\ui\avatar.tsx
- code-craft-frontend\src\components\ui\badge.tsx
- code-craft-frontend\src\lib\date-utils.ts

Create a component for displaying individual notifications in the dropdown. The component should accept a `notification` prop with full notification data, display appropriate icons based on notification type (Follow, Comment, Reply, New Snippet), show formatted notification text with user names and action descriptions, include relative timestamps using existing date utilities, handle click events to mark as read and navigate to relevant content, support unread visual indicators with different styling, and provide hover effects for better UX. Use existing UI components like Avatar, Badge, and proper typography. The component should be responsive and work well in the dropdown list format.

### code-craft-frontend\src\components\notification\NotificationDropdown.tsx(NEW)

References: 

- code-craft-frontend\src\components\notification\NotificationItem.tsx(NEW)
- code-craft-frontend\src\hooks\useNotifications.ts(NEW)
- code-craft-frontend\src\components\ui\scroll-area.tsx

Create a comprehensive notification dropdown component that serves as the main notification interface. The component should use the `useNotifications` hook for data fetching, display a scrollable list of notifications using NotificationItem components, include a header with unread count and "Mark all as read" action, support pagination with "Load more" functionality, handle empty states with appropriate messaging, provide loading skeletons during fetch operations, include error handling with retry options, and support keyboard navigation for accessibility. Use ScrollArea for smooth scrolling and proper dropdown styling. The component should efficiently handle notification state updates and provide smooth animations for state changes.

### code-craft-frontend\src\components\snippet\ReplyBox.tsx(NEW)

References: 

- code-craft-frontend\src\components\ui\textarea.tsx
- code-craft-frontend\src\components\ui\button.tsx
- code-craft-frontend\src\lib\api.ts(MODIFY)

Create a component for composing replies to comments. The component should accept `parentCommentId`, `snippetId`, `onReplySuccess`, and `onCancel` props, provide a textarea for reply content with character count and validation, include submit and cancel buttons with proper loading states, handle form submission with error handling and success feedback, support keyboard shortcuts (Ctrl+Enter to submit, Escape to cancel), auto-focus the textarea when mounted, and integrate with the reply API mutation. Use existing form patterns and validation from the main comment form. The component should be compact and fit well within the comment thread hierarchy.

### code-craft-frontend\src\components\snippet\CommentItem.tsx(MODIFY)

References: 

- code-craft-frontend\src\components\snippet\ReplyBox.tsx(NEW)
- code-craft-frontend\src\types\api.ts(MODIFY)

Transform the existing flat CommentItem into a threaded component that supports nested replies. Add support for `replies` prop as an array of ThreadedComment objects, `depth` prop for indentation control, `onReply` callback for reply functionality, and `maxDepth` prop for nesting limits. Include a "Reply" button that toggles the ReplyBox component, implement proper indentation based on depth using CSS margins or padding, add visual threading indicators (lines or borders) to show reply relationships, support lazy loading of replies with "Show more replies" functionality, and handle the reply state management (showing/hiding reply box). Maintain existing edit/delete functionality while adding the new threading features. The component should recursively render nested replies up to the maximum depth.

### code-craft-frontend\src\components\snippet\CommentList.tsx(MODIFY)

References: 

- code-craft-frontend\src\components\snippet\CommentItem.tsx(MODIFY)
- code-craft-frontend\src\lib\api.ts(MODIFY)
- code-craft-frontend\src\types\api.ts(MODIFY)

Update the CommentList to support threaded comment display and reply functionality. Replace the flat comment fetching with threaded comment API calls using `commentApi.getThreadedComments()`, implement state management for reply operations and comment tree updates, add support for toggling between flat and threaded views, handle reply success callbacks to update the comment tree locally, implement optimistic updates for new replies, add loading states for reply operations, and provide proper error handling for threading operations. Update the comment rendering to use the new threaded CommentItem with proper props passing. Include a toggle button to switch between flat and threaded views for user preference. Maintain backward compatibility with existing comment functionality while adding the new threading features.

### code-craft-frontend\src\components\layout\Header.tsx(MODIFY)

References: 

- code-craft-frontend\src\components\notification\NotificationDropdown.tsx(NEW)
- code-craft-frontend\src\hooks\useNotifications.ts(NEW)
- code-craft-frontend\src\components\ui\badge.tsx

Integrate the notification system into the header by adding a notification bell icon with unread count badge. Add the notification button next to the existing search buttons, use the `useUnreadCount` hook to display real-time unread count, implement a DropdownMenu that contains the NotificationDropdown component, add proper positioning and styling for the notification dropdown, include loading states and error handling for the notification system, add keyboard accessibility for the notification dropdown, and ensure the notification badge updates in real-time when notifications are marked as read. Use the Bell icon from Lucide React and Badge component for the unread count indicator. The notification dropdown should be properly positioned and sized for optimal UX on all screen sizes.

### code-craft-frontend\src\styles\globals.css(MODIFY)

Add CSS custom properties and utility classes for the comment threading and notification systems. Include CSS variables for comment thread indentation (`--comment-indent: 1.5rem`), thread line colors for light and dark themes, notification badge colors and positioning, and comment depth indicators. Add utility classes for comment threading (`.comment-thread`, `.comment-reply`, `.comment-indent-{level}`), notification styling (`.notification-unread`, `.notification-badge`), and threading visual indicators (`.thread-line`, `.reply-indicator`). Ensure all styles work properly in both light and dark themes and provide smooth transitions for interactive elements. The styles should be consistent with the existing design system and Shadcn/ui component styling.