I have created the following plan after thorough exploration and analysis of the codebase. Follow the below plan verbatim. Trust the files and references. Do not re-verify what's written in the plan. Explore only when absolutely necessary. First implement all the proposed file changes and then I'll review all the changes together at the end.

### Observations

The backend has a complete tag system implemented with proper validation, indexing, and API endpoints. The frontend currently lacks any tag functionality but has a solid foundation with Shadcn/ui components, React Query for data fetching, and consistent patterns for forms and API integration. The existing Badge component is perfect for displaying tags, and the Input component can be extended for tag input. The snippets listing page has basic filtering that can be enhanced with tag support. The API client follows consistent patterns that can be extended for tag operations.

### Approach

I'll implement a comprehensive tag system for the frontend by creating reusable tag components using Shadcn/ui, integrating with the existing backend tag API, and enhancing the snippet creation/listing experience. The implementation will include a TagInput component for creating/editing snippets, TagDisplay components for showing tags in cards, tag-based filtering in the snippets listing page, and a TagCloud component for popular tags discovery. I'll extend the existing API layer to support tag operations, update TypeScript interfaces, and ensure proper validation and user experience throughout the tag system.

### Reasoning

I explored the repository structure to understand the codebase organization. I examined the current snippet creation page, listing page, and snippet card to see existing patterns and functionality. I reviewed the backend implementation to understand the complete tag system already available, including API endpoints, validation, and data structures. I analyzed the existing API client, constants, and type definitions to understand frontend patterns. I checked available Shadcn/ui components to see what's available for building the tag system. This gave me a comprehensive understanding of how to implement the frontend tag system following existing conventions and leveraging the robust backend infrastructure.

## Mermaid Diagram

sequenceDiagram
    participant User
    participant CreatePage
    participant TagInput
    participant SnippetsPage
    participant TagFilter
    participant TagCloud
    participant SnippetCard
    participant API

    Note over User,API: Tag Input Flow (Create/Edit Snippet)
    User->>CreatePage: Fill snippet form
    CreatePage->>TagInput: Render tag input component
    User->>TagInput: Type tag name
    TagInput->>API: getPopularTags() for autocomplete
    API-->>TagInput: Popular tags list
    TagInput->>TagInput: Show suggestions dropdown
    User->>TagInput: Select/add tag
    TagInput->>TagInput: Validate tag (length, duplicates)
    TagInput->>CreatePage: Update tags state
    User->>CreatePage: Submit form
    CreatePage->>API: createSnippet({...data, tags})
    API-->>CreatePage: Snippet created with tags

    Note over User,API: Tag-based Filtering Flow
    User->>SnippetsPage: Visit snippets listing
    SnippetsPage->>TagCloud: Render popular tags
    TagCloud->>API: getPopularTags(limit=20)
    API-->>TagCloud: Popular tags with counts
    TagCloud->>TagCloud: Display tags by popularity
    User->>TagCloud: Click on tag
    TagCloud->>SnippetsPage: Add tag to filters
    SnippetsPage->>TagFilter: Update selected tags
    TagFilter->>SnippetsPage: Trigger filter change
    SnippetsPage->>API: getSnippets({tags: [selectedTags]})
    API-->>SnippetsPage: Filtered snippets
    SnippetsPage->>SnippetCard: Render filtered results
    SnippetCard->>SnippetCard: Display tags with TagDisplay

    Note over User,API: Tag Navigation Flow
    User->>SnippetCard: Click on tag in snippet card
    SnippetCard->>SnippetsPage: Navigate with tag filter
    SnippetsPage->>SnippetsPage: Apply tag filter from URL
    SnippetsPage->>API: getSnippets({tags: [clickedTag]})
    API-->>SnippetsPage: Snippets with that tag

## Proposed File Changes

### code-craft-frontend\src\lib\constants.ts(MODIFY)

Add tag-related constants to the API_ENDPOINTS object. Include TAGS section with endpoint for popular tags (`/snippets/tags/popular`). Update API_LIMITS to include tag-related limits: `MAX_TAGS_PER_SNIPPET: 10`, `MAX_TAG_LENGTH: 30`, `MIN_TAG_LENGTH: 2` to match backend validation. These constants will ensure consistency between frontend validation and backend limits.

### code-craft-frontend\src\types\api.ts(MODIFY)

Update the Snippet interface to include the `tags` field as an optional array of strings. Add TagData interface with `tag: string` and `count: number` for popular tags response. Add CreateSnippetRequest and UpdateSnippetRequest interfaces that include the tags field. Add PopularTagsResponse interface for the popular tags API response. These type definitions will ensure type safety across the tag system components.

### code-craft-frontend\src\lib\api.ts(MODIFY)

References: 

- code-craft-frontend\src\lib\constants.ts(MODIFY)

Update the snippetApi object to support tags in all relevant functions. Modify `getSnippets` to accept a `tags` parameter as an array of strings and pass it to the API. Update `createSnippet` and `updateSnippet` functions to include the tags field in the request payload. Add a new `getPopularTags` function that calls the `/snippets/tags/popular` endpoint with optional limit parameter. Ensure all functions maintain the existing error handling patterns and type safety.

### code-craft-frontend\src\components\ui\command.tsx(NEW)

Create a Shadcn/ui Command component using `npx shadcn-ui@latest add command`. This component will be used for the tag input autocomplete functionality, allowing users to search and select from existing tags while typing. The command component should support keyboard navigation, filtering, and custom rendering of tag suggestions.

### code-craft-frontend\src\components\ui\popover.tsx(NEW)

Create a Shadcn/ui Popover component using `npx shadcn-ui@latest add popover`. This component will be used to display the tag suggestions dropdown in the TagInput component. The popover should support proper positioning, accessibility, and smooth animations.

### code-craft-frontend\src\components\snippet\TagInput.tsx(NEW)

References: 

- code-craft-frontend\src\components\ui\badge.tsx
- code-craft-frontend\src\components\ui\input.tsx
- code-craft-frontend\src\components\ui\command.tsx(NEW)
- code-craft-frontend\src\components\ui\popover.tsx(NEW)
- code-craft-frontend\src\lib\constants.ts(MODIFY)

Create a comprehensive TagInput component for adding/editing tags in snippet forms. The component should accept `value` (array of strings), `onChange` callback, `placeholder`, and `disabled` props. Implement tag input functionality with: input field for typing new tags, Enter/comma key to add tags, backspace to remove last tag when input is empty, tag validation (length, format, duplicates), visual feedback for invalid tags, and autocomplete suggestions using popular tags. Use Badge components to display added tags with remove buttons. Include proper accessibility attributes, keyboard navigation, and error states. Limit tags to MAX_TAGS_PER_SNIPPET and validate tag length according to constants.

### code-craft-frontend\src\components\snippet\TagDisplay.tsx(NEW)

References: 

- code-craft-frontend\src\components\ui\badge.tsx

Create a TagDisplay component for showing tags in snippet cards and detail views. The component should accept `tags` array and optional `onTagClick` callback for filtering. Display tags using Badge components with consistent styling, support different sizes (sm, md, lg), handle empty tags gracefully, and provide click functionality for tag-based filtering. Include hover effects and proper spacing between tags. The component should be responsive and work well in different layouts (card footer, detail page, etc.).

### code-craft-frontend\src\components\snippet\TagCloud.tsx(NEW)

References: 

- code-craft-frontend\src\components\ui\badge.tsx
- code-craft-frontend\src\lib\api.ts(MODIFY)

Create a TagCloud component for displaying popular tags with usage counts. The component should fetch popular tags using React Query, display tags with different sizes based on usage frequency, support click functionality for filtering snippets by tag, include loading and error states, and provide a responsive layout that works on all devices. Use Badge components with different variants to show tag popularity visually. Include a refresh button and proper error handling. The component should be reusable across different pages (discover, snippets listing, etc.).

### code-craft-frontend\src\components\snippet\TagFilter.tsx(NEW)

References: 

- code-craft-frontend\src\components\ui\badge.tsx
- code-craft-frontend\src\components\ui\input.tsx
- code-craft-frontend\src\components\snippet\TagCloud.tsx(NEW)

Create a TagFilter component for the snippets listing page sidebar. The component should accept `selectedTags` array, `onTagsChange` callback, and `availableTags` array. Provide functionality to: select/deselect multiple tags for filtering, display selected tags with remove buttons, show popular tags as quick selection options, include a search input for finding specific tags, and clear all filters button. Use Badge components for tag display and maintain consistent styling with the rest of the application. Include proper accessibility and keyboard navigation support.

### code-craft-frontend\src\app\snippets\create\page.tsx(MODIFY)

References: 

- code-craft-frontend\src\components\snippet\TagInput.tsx(NEW)
- code-craft-frontend\src\lib\api.ts(MODIFY)

Integrate the TagInput component into the snippet creation form. Add a new `tags` state variable initialized as an empty array. Include the TagInput component in the form between the description and language fields with proper label and validation. Update the `createSnippet` mutation to include tags in the request payload. Add client-side validation for tags (max count, length validation) and display appropriate error messages. Ensure the form submission includes the tags data and handles tag-related validation errors from the API. Maintain the existing form structure and styling while seamlessly integrating tag functionality.

### code-craft-frontend\src\app\snippets\page.tsx(MODIFY)

References: 

- code-craft-frontend\src\components\snippet\TagFilter.tsx(NEW)
- code-craft-frontend\src\components\snippet\TagCloud.tsx(NEW)
- code-craft-frontend\src\lib\api.ts(MODIFY)

Enhance the snippets listing page with comprehensive tag-based filtering. Add `selectedTags` state for tracking active tag filters. Integrate the TagFilter component in the existing filter sidebar. Update the snippets query to include selected tags in the API call. Add TagCloud component above the snippets grid for popular tags discovery. Modify the search functionality to work alongside tag filtering. Include clear visual indicators for active tag filters and provide easy ways to remove individual tags or clear all filters. Ensure the URL reflects the current tag filters for shareable links and browser back/forward navigation. Maintain the existing responsive design and loading states.

### code-craft-frontend\src\components\snippet\SnippetCard.tsx(MODIFY)

References: 

- code-craft-frontend\src\components\snippet\TagDisplay.tsx(NEW)

Integrate tag display into the SnippetCard component. Add the TagDisplay component to show snippet tags in the card footer, positioned appropriately within the existing layout. Implement click functionality on tags to trigger filtering (navigate to snippets page with tag filter). Ensure tags are displayed responsively and don't break the card layout on different screen sizes. Handle cases where snippets have no tags gracefully. Maintain the existing card structure and styling while adding tag functionality. Include proper spacing and visual hierarchy for the tags within the card design.

### code-craft-frontend\src\app\snippets\[id]\page.tsx(MODIFY)

References: 

- code-craft-frontend\src\components\snippet\TagDisplay.tsx(NEW)
- code-craft-frontend\src\components\snippet\TagInput.tsx(NEW)

Enhance the snippet detail page to display tags prominently and provide tag-based navigation. Add TagDisplay component to show the snippet's tags near the title/metadata section. Implement click functionality on tags to navigate to filtered snippets listing. Include an edit mode that uses TagInput component when the user owns the snippet. Add a "Related Snippets" section that shows other snippets with similar tags. Ensure the tag display is responsive and integrates well with the existing page layout. Handle loading and error states appropriately when fetching related snippets.

### code-craft-frontend\src\hooks\useTagSearch.ts(NEW)

References: 

- code-craft-frontend\src\lib\api.ts(MODIFY)
- code-craft-frontend\src\hooks\useDebounce.ts

Create a custom hook for tag search functionality that can be reused across components. The hook should provide debounced search for tags, manage loading states, handle popular tags fetching, and provide filtered results based on user input. Include functions for: `searchTags(query)` with debouncing, `getPopularTags(limit)` for fetching popular tags, `isLoading` state management, and error handling. Use React Query for caching and efficient data fetching. The hook should be optimized for performance and provide a clean API for components to consume tag data.

### code-craft-frontend\src\utils\tagUtils.ts(NEW)

References: 

- code-craft-frontend\src\lib\constants.ts(MODIFY)

Create utility functions for tag validation and manipulation. Include functions for: `validateTag(tag)` to check tag format and length, `normalizeTags(tags)` to remove duplicates and normalize case, `formatTagsForUrl(tags)` and `parseTagsFromUrl(urlParams)` for URL handling, `getTagColor(tag)` for consistent tag coloring, and `filterTagsByQuery(tags, query)` for search functionality. These utilities should follow the same validation rules as the backend and provide consistent tag handling across the application.